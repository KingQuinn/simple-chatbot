# Simple Chatbot

This is a basic CLI chatbot project initialized using `uv`.

## How to Run

1. Make sure you have `uv` installed.
2. Run the chatbot:
   ```bash
   uv run main.py
   ```

## Chatbot Features

- Interactive CLI interface
- Basic greeting responses
- Exit with "quit" or "exit"
